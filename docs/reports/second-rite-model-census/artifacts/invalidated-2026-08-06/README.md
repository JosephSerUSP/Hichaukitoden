# Invalidated 2026-08-06 contact sheets

`invalidated-overview.png` is a compact tracked composite made from the actual decision sheets produced by the first in-engine census pass and supplied during the follow-up audit. It is preserved because it demonstrates why the original subjective review was retracted without committing the full disposable `/out` archive.

Visible failure modes include models absent from labeled tiles, severe/near-plane polygon artifacts around opening assets, environment-dominated floor/wall captures, repeated `MISSING` cards, and a contextual background whose obsolete `dungeon_default` texture overwhelms model diagnosis.

`artifact-manifest.json` records the committed overview hash plus the original source hashes/byte sizes for the five supplied sheets from which the overview was assembled.

Root-cause analysis and the corrective protocol are documented in `../../in-engine-review.md`.

**This image must not be used as evidence for promotion/rejection decisions.**
