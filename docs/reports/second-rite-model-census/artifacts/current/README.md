# Current corrected-run decision evidence

This directory is populated by `python tools/asset-production/review_model_census.py` after a harness-v2 run. The corrective commit intentionally does **not** fabricate current evidence before the LÖVE rerun has happened.

After a successful rerun, commit the generated `run.json`, `index.json`, `captures.jsonl`, `review.csv`, `smoke.json`, `diagnostics.json`, `artifact-manifest.json`, `contact-sheets/`, and `smoke/` contents alongside any new subjective review conclusions.
