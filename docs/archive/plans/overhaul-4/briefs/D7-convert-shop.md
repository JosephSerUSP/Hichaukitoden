# D7: Convert Shop

**Context:** SPEC S6. Proves the use of formulas and conditions (gold checks) within a menu context.

**Role:** Jules-shippable.

## Acceptance Criteria
- [ ] Convert the `Shop` scene into data hooks in `scenes.json`.
- [ ] Implement conditional checks (e.g., gold checks, inventory capacity) using `IF` and formulas.
- [ ] The scene UI-golden log must be byte-identical before and after conversion.

**Gates:** G1, G3 + UI-golden.
