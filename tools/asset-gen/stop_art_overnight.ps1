param(); $root=(Resolve-Path (Join-Path $PSScriptRoot '..\..')).Path; $pidPath=Join-Path $root 'tools\asset-gen\out\art-overnight-20260807\run.pid'; if(Test-Path $pidPath){$id=[int](Get-Content $pidPath -Raw); if(Get-Process -Id $id -ErrorAction SilentlyContinue){taskkill.exe /PID $id /T /F|Out-Null; Write-Output "Stopped campaign worker PID $id; Forge left running."}else{Write-Output 'Worker already stopped.'}; Remove-Item $pidPath -Force}else{Write-Output 'No campaign PID file.'}
